services:
  - type: web
    name: shinzooh-final-v
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt --no-cache-dir
    startCommand: gunicorn -w 2 -k uvicorn.workers.UvicornWorker main:app -t 120 -b 0.0.0.0:$PORT
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: XAI_API_KEY
        sync: false

