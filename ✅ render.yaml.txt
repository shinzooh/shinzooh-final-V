services:
  - type: web
    name: shinzooh-enhanced-v2
    env: python
    plan: free
    buildCommand: pip install -r requirements_final.txt --no-cache-dir
    startCommand: uvicorn main_final:app --host 0.0.0.0 --port $PORT --workers 2
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: XAI_API_KEY
        sync: false
      - key: CLAUDE_API_KEY
        sync: false

